import 'package:flutter/material.dart';

class UserTile extends StatelessWidget {
  const UserTile({this.uid, this.username});

  final String uid;
  final String username;

  @override
  Widget build(BuildContext context) {
    // print("$uid -- $username");
    return Container();
  }
}
